# Isolated Agent Evaluation

Follow the task prompt exactly. Do not use network tools or inspect paths
outside this fixture.

This fixture is read-only. Return the computed report directly; do not create
an artifact or run a validator.
